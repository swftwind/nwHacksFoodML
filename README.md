use command below to install dependencies:

pip install opencv-python torch torchvision requests pillow
pip install ultralytics

for me on this machine, there are multiple versions of python, so this command below must be run to specify to use the local interpreter for this project:

.\venv\Scripts\activate